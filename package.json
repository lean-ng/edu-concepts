{
  "scripts": {
    "start": "serve src",
    "build": "rimraf dist && copyfiles -u 1 src/**/* dist/edu-concepts"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "serve": "^14.0.1",
    "rimraf": "^3.0.2"
  }
}
